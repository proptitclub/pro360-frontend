import { Container } from '@mui/material';
import Head from 'next/head';
import React, { Fragment } from 'react';

const Bookmark = () => {
  return (
    <Fragment>
      <Head>
        <title>Bookmark | Pro360</title>
      </Head>

      <Container maxWidth='xl'>Bookmark</Container>
    </Fragment>
  );
};

export default Bookmark;
