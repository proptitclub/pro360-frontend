import { Container } from '@mui/material';
import Head from 'next/head';
import React, { Fragment } from 'react';

const Notification = () => {
  return (
    <Fragment>
      <Head>
        <title>Notification | Pro360</title>
      </Head>

      <Container maxWidth='xl'>Notification</Container>
    </Fragment>
  );
};

export default Notification;
